# The CLI discovery dirs are generated locally by ./scripts/link.sh (they hold
# only symlinks back into claude/ and opencode/, which are the source of truth).
# Not tracked — run the linker after cloning to enable in-repo runs.
.claude/
.opencode/

.DS_Store

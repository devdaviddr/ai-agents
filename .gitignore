# Local CLI state written when running Claude Code / opencode inside the repo.
# The agent/skill symlinks under .claude/ and .opencode/ ARE tracked — only
# ignore ephemeral settings and session files here.
.claude/settings.local.json
.claude/settings.json
.claude/.sessions/
.opencode/*.json
.opencode/cache/

.DS_Store
